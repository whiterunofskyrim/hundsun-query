server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hundsun?serverTimezone=GMT%2b8
    username: root
    password:

mybatis:
  type-aliases-package: com.example.businessearch.pojo
  mapper-locations: classpath:mapper/*.xml
